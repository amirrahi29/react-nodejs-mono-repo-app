# Dev — Ingress band; LoadBalancer IP se access. Ingress on karte waqt apna host yahan bharo.
service:
  type: LoadBalancer

ingress:
  enabled: false
  className: nginx
  hosts: []

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
